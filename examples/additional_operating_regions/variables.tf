# This example takes no inputs — the additional operating Regions are set inline
# in main.tf to keep the demonstration self-contained. A file is kept for parity
# with the other examples and as the place to add variables if the example grows.
